{
  "version": 2,
  "environments": {
    "production": {
      "buildCommand": "python -m venv --copies /opt/venv && source /opt/venv/bin/activate && pip install -r requirements.txt",
      "startCommand": "gunicorn app.wsgi"
    }
  }
}

